# Simcenter Flotherm 2504 (2025.1) notes

Latest tested. The Win32 GUI automation playback path in `base/` works
unchanged. Headless batch mode remains broken upstream (see
`known_issues.md`).

This file exists so the layer is non-empty and `verify_skills_layout`
passes.
