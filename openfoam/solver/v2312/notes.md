# OpenFOAM v2312 (December 2023) notes

Stable. No documented overrides vs `base/`.

This file exists so the layer is non-empty and `verify_skills_layout`
passes. Per-release quirks land here as discovered.
