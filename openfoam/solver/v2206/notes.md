# OpenFOAM v2206 notes

Older but still common in production. The original 343-tutorial test
suite that produced `base/reference/success_patterns.md` and
`base/reference/failure_patterns.md` ran on v2206.

This file exists so the layer is non-empty and `verify_skills_layout`
passes. Per-release deltas vs v2312/v2406 land here.
