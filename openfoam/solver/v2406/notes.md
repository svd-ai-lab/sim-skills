# OpenFOAM v2406 (June 2024) notes

Latest stable line. The patterns in `base/reference/` and the bashrc
sourcing in sim-cli's openfoam driver work unchanged on v2406.

This file exists so the layer is non-empty and `verify_skills_layout`
passes. Per-release quirks (new solvers, deprecated dictionary keys)
land here as discovered.
