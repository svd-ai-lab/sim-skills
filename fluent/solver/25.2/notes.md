# Fluent 25R2 (v252) notes

No 25R2-specific overrides yet — the EX-01 mixing-elbow workflow runs
unchanged on 24R2 / 25R1 / 25R2 with the patterns in `base/snippets/`
plus the SDK layer `sdk/0.38/`.

This file exists so the layer is non-empty and `verify_skills_layout`
passes. Real per-release quirks land here when discovered.
