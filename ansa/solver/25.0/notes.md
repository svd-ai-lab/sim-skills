# BETA CAE ANSA 25.0 notes

Latest tested. The headless batch path
(`ansa64.bat -execscript "script.py|main()" -nogui`) and the FEM
input/output patterns in `base/` work unchanged on 25.0.

This file exists so the layer is non-empty and `verify_skills_layout`
passes.
