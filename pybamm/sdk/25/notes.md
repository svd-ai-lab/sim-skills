# PyBaMM 25.x notes

25.x is the latest tested line. As of writing, the public API for
`pybamm.lithium_ion.SPM/SPMe/DFN`, `Simulation`, and `Experiment` is
backward-compatible with 24.x — `base/snippets/` work unchanged.

## Notes

This file exists so the layer is non-empty and `verify_skills_layout`
passes. Real overrides land here as 25.x diverges.
